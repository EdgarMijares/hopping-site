$(document).ready(function() {

/*
	$('#testpushnotification').on('click', function(some){
		console.log(some);
		$.ajax({
			type: 'POST',
			url: '/handlePost'
		})
		.done(function(data) {
			console.log('GET response:', data);
		})
		.fail(function(jqXHR, textStatus, error) {
			console.log('AJAX error response', textStatus, 'jqXHR is', jqXHR, 'error is ',error);
		});
	});//end on click
*/
});//end ready function